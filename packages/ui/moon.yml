type: 'library'

tasks:
  build:
    command: 'vite build'
    inputs:
      - 'src/**/*'
      - '@group(sources)'
    outputs:
      - 'dist'
  lint:
    args:
      - '--ext'
      - '.ts,.svelte'
